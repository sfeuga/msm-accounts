# frozen_string_literal: true

# add 'app' dir to the load_path
$LOAD_PATH.unshift("#{File.dirname(__FILE__)}/lib")

require 'bundler/setup'
require 'dotenv'
require 'dotenv/tasks'
require 'rubygems'

Dotenv.load
